# 🖥️ mygit — Your Own Git in Python

`mygit` is a simplified version control system implemented in Python. It mimics essential Git commands like `init`, `add`, `commit`, and `checkout`, giving you a hands-on understanding of how Git works under the hood.
